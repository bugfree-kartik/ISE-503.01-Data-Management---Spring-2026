# Frontend — used locally. Copy to .env.local and fill in values.
# Leave VITE_API_URL empty for local dev (Vite proxy handles /api → :3001).
# In production, set to your deployed backend URL (no trailing slash).

# VITE_API_URL=https://sbu-dining-api.onrender.com
VITE_API_URL=
